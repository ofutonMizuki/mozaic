// Positive: returning a reference derived from a parameter is safe (caller owns the referent).
struct Cell { v: i32; }
function pick(b: &Cell): &Cell { return b; }
function main() {
  let b: Cell = Cell { v: 42 };
  let r: &Cell = pick(&b);
  stdout.println(r.v);   // 42 (auto-deref)
}
