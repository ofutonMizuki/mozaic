// Negative: returning a reference to a local would dangle.
struct Cell { v: i32; }
function bad(): &Cell {
  let local: Cell = Cell { v: 1 };
  return &local;
}
function main() { stdout.println(1); }
