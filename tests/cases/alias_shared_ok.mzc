// Positive: the same variable may be shared-borrowed (&) multiple times in one call.
struct Point { x: i32; y: i32; }
function sumX(a: &Point, b: &Point): i32 { return a.x + b.x; }
function main() {
  let p: Point = Point { x: 5, y: 9 };
  stdout.println(sumX(&p, &p));   // 10
}
